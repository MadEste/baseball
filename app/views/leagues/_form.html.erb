<%= simple_form_for(@league) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :leagueID, hint: 'Six digits Max! Numbers only!', as: :numeric, input_html: { maxlength: 6 } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
